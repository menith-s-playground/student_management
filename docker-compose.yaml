services:
  container:
    environment:
      MARIADB_ROOT_PASSWORD: mariasql
    ports:
      - 9000:3306
    volumes:
      - ~/mariadb:/var/lib/mariadb
    image: mariadb
    container_name: objective_mccarthy